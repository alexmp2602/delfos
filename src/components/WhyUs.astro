---
import IconBolt from "./icons/IconBolt.astro";
import IconBulb from "./icons/IconBulb.astro";
import IconHandshake from "./icons/IconHandshake.astro";

const benefits = [
  {
    icon: IconBolt,
    title: "Velocidad",
    desc: "Optimizamos tiempos y procesos para que tu proyecto esté online antes que la competencia.",
  },
  {
    icon: IconBulb,
    title: "Diseño estratégico",
    desc: "Creamos experiencias digitales a medida que potencian la identidad y los objetivos de tu marca.",
  },
  {
    icon: IconHandshake,
    title: "Atención personalizada",
    desc: "Te acompañamos en cada etapa, escuchando y adaptándonos a tus necesidades y visión.",
  },
];
---

<section
  id="whyus"
  class="scroll-offset px-6 py-16 text-offwhite transition-soft"
  aria-labelledby="titulo-whyus"
>
  <div class="max-w-6xl mx-auto text-center">
    <h2 id="titulo-whyus" class="text-3xl md:text-5xl font-deco text-gold mb-6">
      ¿Por qué elegir Delfos Studio?
    </h2>

    <p
      class="text-base sm:text-lg text-[#ededed] max-w-2xl mx-auto mb-14 leading-relaxed"
    >
      Creamos soluciones digitales con creatividad, estrategia y cercanía.
      Aceleramos tus resultados y potenciamos tu marca, acompañándote en cada
      paso.
    </p>

    <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-3">
      {
        benefits.map(({ icon: Icon, title, desc }, i) => (
          <article
            class="group p-6 bg-[#1a1a1a] rounded-xl text-center shadow-md transition-transform duration-300 hover:scale-[1.02] hover:shadow-gold-glow focus:outline-none focus-visible:ring-2 focus-visible:ring-gold animate-fadein"
            style={`animation-delay: ${0.05 + i * 0.06}s`}
            tabindex={0}
            aria-label={title}
          >
            <div class="mb-4 text-gold flex justify-center">
              <Icon class="w-10 h-10" />
            </div>
            <h3 class="text-xl font-semibold mb-2 group-hover:text-gold transition-colors">
              {title}
            </h3>
            <p class="text-sm text-[#dcdcdc] leading-relaxed">{desc}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
